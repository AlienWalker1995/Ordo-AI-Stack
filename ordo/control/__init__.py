"""The control plane: ops-controller's process (`ordo serve`, serve.py), its HTTP API (api.py), the
GPU scheduler and broker, the audit log and the lease state. Imports ordo.render only, never
ordo.host.
"""
