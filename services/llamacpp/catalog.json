{
  "cards": [
    {
      "id": "llamacpp",
      "name": "llama.cpp (GPU)",
      "order": 10,
      "port": 8080,
      "url": "http://localhost:8080",
      "check": "http://llamacpp:8080/health",
      "has_gpu": true,
      "plugin": null,
      "category": "inference",
      "background": true,
      "ops_service": "llamacpp",
      "hint": "Backend-only; use model-gateway :11435 from host. Run: docker compose up -d llamacpp",
      "notes": "Core service (plugin null = always visible). The compose service is defined by the substrate (ordo/compose.py); this dir exists to carry its dashboard card."
    }
  ]
}
