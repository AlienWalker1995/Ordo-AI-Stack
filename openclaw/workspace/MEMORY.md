# Memory

Optional long-form notes for this workspace. Add durable facts, preferences, or session summaries here when useful.

Structured memory (search / recall) also uses OpenClaw’s SQLite store under `data/openclaw/memory/` outside this file.
