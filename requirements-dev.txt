# Pinned dev/CI deps for the v2 substrate — pinned (not floated) so CI is reproducible and a
# ruff release can't silently surface new lint on an unrelated change (a known stack CI gotcha).
pyyaml==6.0.3
pytest==8.3.4
ruff==0.8.6
# ordo/model_registry.py (the runtime model state the control plane reads) is substrate code and
# declares its schema with pydantic, so importing ordo.control needs it. It was reaching CI only
# because a now-removed step installed services/ops-api/requirements.txt as a side effect; pinned
# to the same version the ops-controller image installs so CI tests what ships.
pydantic==2.13.4
# The control plane's transport is FastAPI (ordo/control.py ControlPlane.app), and the ASGI tests
# build the real app and drive it through starlette's TestClient, which needs httpx. Same versions
# as the ops-controller image installs.
fastapi==0.141.1
httpx==0.28.1
