# Pinned dev/CI deps for the v2 substrate — pinned (not floated) so CI is reproducible and a
# ruff release can't silently surface new lint on an unrelated change (a known stack CI gotcha).
pyyaml==6.0.3
pytest==8.3.4
ruff==0.8.6
# pydantic is FastAPI's model layer (below). Pinned to the same version the ops-controller image
# installs, so CI resolves the same pair that ships rather than whatever pip picks.
pydantic==2.13.4
# The control plane's transport is FastAPI (ordo/control/api.py ControlPlane.app), and the ASGI tests
# build the real app and drive it through starlette's TestClient, which needs httpx. Same versions
# as the ops-controller image installs.
fastapi==0.141.1
httpx==0.28.1
